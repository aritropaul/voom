# Voom Secrets
# Copy this file to .env and fill in your values.
# These are entered via Settings UI in the app, but documented here for reference.

# Cloudflare Worker URL for cloud sharing
SHARE_WORKER_URL=https://your-worker.workers.dev

# API secret for authenticating with the Worker
SHARE_API_SECRET=your-api-secret-here
