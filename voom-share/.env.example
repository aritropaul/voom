# Cloudflare Worker configuration
# Copy this to wrangler.toml and fill in your values

# D1 database ID (from `npx wrangler d1 create voom-share-db`)
D1_DATABASE_ID=

# API secret (generate with `openssl rand -hex 32`, set with `npx wrangler secret put API_SECRET`)
API_SECRET=
